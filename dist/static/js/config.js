// default config for local dev env
// will be replaced by env variables when running in a Docker container
window.env = {
  API_ROOT: 'https://bittrex.com/api/v1.1',
  LOCATION: window.location.href
}
